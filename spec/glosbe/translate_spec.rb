require "spec_helper"

RSpec.describe Glosbe::Translate do
  it "has a version number" do
    expect(Glosbe::Translate::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
